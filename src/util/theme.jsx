import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6894c1',
    },
    success: {
      main: '#469F4B',
    },
    error: {
      main: '#D9665F',
    },
  },
});

export default theme;
